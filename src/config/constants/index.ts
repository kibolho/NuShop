import Constants from 'expo-constants';
export const BASE_URL = Constants.manifest?.extra?.BASE_URL;
export const BEARER_TOKEN = Constants.manifest?.extra?.BEARER_TOKEN;
export const APP_VERSION = Constants.manifest?.version;
